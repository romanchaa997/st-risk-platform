# PERFORMANCE_CHECKLIST

Контекст: `st-risk-platform` — Python / FastAPI сервисы, ClickHouse как хранилище, запуск через `docker-compose`.  
Цель: стабильная работа под нагрузкой, понятные SLA, наблюдаемость и готовность к дальнейшему масштабированию.

## 1. SLA

- [ ] Определены SLA для ключевых endpoint'ов (risk assessment, features, model prediction).
- [ ] Для ключевых endpoint'ов задана цель: **P95 latency < 200–300 мс** при пиковой нагрузке. 
- [ ] Для сервисов и ключевых endpoint'ов задана цель: **error rate < 0.1%**.
- [ ] Заданы пределы по ресурсам: допустимая утилизация CPU/RAM для контейнеров FastAPI и ClickHouse.
- [ ] SLA и целевые значения задокументированы (например, в `docs/SLA.md` или Wiki репозитория).

## 2. Observability

- [ ] Все FastAPI‑сервисы экспонируют метрики (Prometheus / аналог) и/или интегрированы с APM.
- [ ] Для каждого ключевого endpoint'а есть дашборды с **P50/P95/P99 latency**, **throughput (req/s)**, **error rate**.
- [ ] Есть дашборды по ресурсам контейнеров: CPU, RAM, диск, сеть.
- [ ] Есть метрики ClickHouse: время ответов запросов, количество запросов, ошибки, нагрузка на диск/CPU.
- [ ] Включён логирование на уровне запросов/ответов (коррелируется с трейсами/метриками по trace_id/request_id).
- [ ] Настроены алерты по SLA: превышение P95 latency, рост error rate, исчерпание ресурсов.

## 3. Caching

*(если кэш ещё не внедрён — отмечаем как TODO и создаём задачи)*

- [ ] Определён список тяжёлых/частых операций, которые выгодно кэшировать (агрегации, справочники, конфигурации).
- [ ] Выбран внешний кэш (например, Redis) и добавлен в инфраструктуру.
- [ ] Для кэша используется **cache-aside** паттерн:
  - сначала чтение из кэша;
  - при miss — чтение из ClickHouse/сервиса и запись в кэш.
- [ ] Для ключей заданы TTL с рандомизацией (джиттер), чтобы избежать одновременного истечения.
- [ ] Реализованы механизмы защиты от cache stampede (lock / stale-while-revalidate / single-flight).
- [ ] Есть базовые метрики кэша: hit ratio, количество операций, ошибки подключения.

## 4. Database (ClickHouse)

- [ ] Схема ClickHouse оптимизирована под основные сценарии чтения/записи (primary key, партиционирование, порядок сортировки).
- [ ] Для частых/тяжёлых запросов проведён анализ и оптимизация (избегаем full scan там, где он не нужен).
- [ ] Используется connection pooling для доступа к ClickHouse из FastAPI‑сервисов.
- [ ] Ограничены выбираемые поля и установлен разумный `LIMIT` для API‑запросов.
- [ ] Есть дашборды с:
  - временем выполнения основных запросов,
  - количеством запросов,
  - ошибками/отклонёнными запросами.
- [ ] Есть алерты на деградацию ClickHouse (время запросов, рост ошибок, проблемы с диском/CPU).

## 5. Application & Code

- [ ] Проведён профайлинг «горячих» участков: циклы по большим коллекциям, сортировки, агрегации, сериализация/десериализация JSON.
- [ ] В горячих местах избегаются алгоритмы с **O(n²)** при больших `n`, заменены на **O(n log n)** или лучше.
- [ ] Нет лишних проходов по данным и дублирующих вычислений в пределах одного запроса.
- [ ] Убраны дублирующиеся запросы к ClickHouse и внешним сервисам (повторное использование результатов/кэш).
- [ ] В FastAPI‑сервисах максимально используется async I/O, блокирующие операции вынесены в отдельные воркеры/пулы.
- [ ] Конфигурация (таймауты, лимиты, размеры пулов) вынесена в переменные среды и документирована.

## 6. Scaling (docker-compose / базовый уровень)

- [ ] В `docker-compose.yml` задокументированы рекомендации по количеству реплик для сервисов при разных уровнях нагрузки.
- [ ] Для HTTP‑слоя используется reverse proxy/load balancer (например, Nginx/Traefik) перед FastAPI‑контейнерами.
- [ ] Настроены таймауты и лимиты соединений в прокси и в приложении.
- [ ] Есть минимальный набор сценариев нагрузочного тестирования (например, `locust`/`k6`) с проверкой SLA (P95 latency, error rate).
- [ ] Определена стратегия scale‑up (увеличение ресурсов контейнеров) и scale‑out (увеличение числа реплик) для ручного масштабирования.

## 7. Resilience

- [ ] Для обращений к ClickHouse и внешним сервисам используются таймауты и retry с backoff (без бесконечных зависаний).
- [ ] Введены circuit breaker / fallback‑механизмы для критичных зависимостей (по возможности).
- [ ] Обработаны и логируются ошибки на границе API (нет «тихих» падений).
- [ ] Есть базовые health‑check endpoint'ы (`/health`, `/ready`), учитывающие состояние критичных зависимостей (ClickHouse, кэш).

## 8. Future: K8s / HPA / backlog-based autoscaling

*(целевая картина на случай миграции с docker-compose на Kubernetes/облако)*

- [ ] Описана целевая стратегия деплоя в Kubernetes (отдельные deployment'ы для gateway, feature-service, model-service, ClickHouse).
- [ ] Для каждого сервиса определены кастомные метрики для HPA (например, `requests_per_pod`, `backlog_per_pod`).
- [ ] Для очередей/таск‑воркеров (если будут добавлены) задана модель autoscaling по backlog:
  - `backlog_per_instance = queue_length / running_instances`;
  - `acceptable_backlog_per_instance = acceptable_latency / avg_processing_time`.
- [ ] Задано правило масштаба:
  - scale out, если `backlog_per_instance` выше порога X в течение Y секунд;
  - scale in, если `backlog_per_instance` ниже порога Z в течение дольше времени (более мягкий scale in).
- [ ] Планируется интеграция с HPA на основе кастомной метрики (через Prometheus Adapter / CloudWatch и т.п.).
- [ ] Есть описание целевых дашбордов/алертов для autoscaling (пороговые значения backlog, latency, количество реплик).

---

**Рекомендация:** для каждого чекбокса отметь текущее состояние (✅ сделано, 🔄 в процессе, ⏳ в планах). Начни с разделов **SLA** и **Observability** — они критичны для принятия решений по остальным оптимизациям.
